import React from "react";
import { Container } from "react-bootstrap";

const GameList = ({ games }) => {
  return (
    <Container>
      <h1 className="text-center mt-4">Danh sách Game</h1>
    </Container>
  );
};

export default GameList;
